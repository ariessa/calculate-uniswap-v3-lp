const calculate_lp = require('../lib/calculate_lp');

describe("function calculate_lp", () => {
    beforeAll(() => {
        // TODO: Deploy 2 dummy tokens with different liquidity
        // TODO: Create liquidity pool
    });
      
    test("returns error if liquidity pool's address in invalid", () => {
        console.log("calculate_lp: ", calculate_lp);
    });

    // test("can calculate the value of valid lp tokens", () => {
    // });

});