const liquidity_pool = require('../../lib/liquidity_pool');
const constants = require("../../lib/constants");

describe("function calculate_lp", () => {
    test("returns error if liquidity pool's address is invalid", async () => {
    });

    // test("returns error if user's address is invalid", async () => {
    //     console.log("calculate_lp: ", calculate_lp);
    // });

    // test("returns error if liquidity pool does not exist", async () => {
    //     console.log("calculate_lp: ", calculate_lp);
    // });

    // test("can calculate the value of valid lp tokens", async () => {
    // });
});